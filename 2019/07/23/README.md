```
transform(map, values,volume)                               :  4666 ns total,  1.14 ns per input key
neon_transform(map, values,volume)                          :  5541 ns total,  1.35 ns per input key
neon_transform_ascii(map, values,volume)                    :  2916 ns total,  0.71 ns per input key
```
