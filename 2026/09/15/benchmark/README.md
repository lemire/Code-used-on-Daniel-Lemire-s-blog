# The cost of subnormal floating-point numbers

Under Linux and macOS, you may run:

```
cmake -B build
cmake --build build
./build/benchmark
```

To use a specific compiler:

```
CXX=g++-15 cmake -B build
```
